# ButterflAI requirements.txt
# Colab-compatible pinned dependencies.
# Note: torch and torchvision are pre-installed in Colab; listed here for
# local development and CI. Colab users: run `setup()` which handles installs.

# Core scientific stack (pre-installed in Colab — listed for local/CI)
numpy>=1.24,<2.0
pandas>=2.0
scipy>=1.11
matplotlib>=3.7

# Deep learning (pre-installed in Colab with GPU support)
# torch>=2.1
# torchvision>=0.16
# lightning>=2.1

# Data access
requests>=2.31
tqdm>=4.66

# Scientific/analysis packages (mirrors environment.yml)
numba==0.63.1
scikit-learn==1.8.0
scikit-image==0.26.0
xarray==2025.12.0
dask==2025.12.0
bottleneck==1.6.0
h5netcdf==1.7.3
hdf5plugin==6.0.0
opencv-python==4.12.0

# Solar physics / visualization
sunpy==7.1.0

# Notebook extensions (Colab use is optional)
ipykernel==8.21.0
ipywidgets==8.1.8
jupyter_contrib_nbextensions==6.5.4

# Logging / experiment tracking
# wandb==0.23.1

# HuggingFace ecosystem + utilities
# datasets
# huggingface_hub==0.36.0
# hf_transfer==0.1.9

# Other ML utilities
# peft==0.18.1
# torchmetrics==1.8.2
# s3fs==2026.1.0
# einops==0.8.1
# timm==1.0.24
# boto3==1.42.19
